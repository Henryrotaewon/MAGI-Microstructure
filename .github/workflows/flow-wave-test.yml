name: MAGI Flow Wave Test

on:
  push:
    paths:
      - 'src/flow_wave/**'
      - 'tests/test_flow_wave.py'
      - '.github/workflows/flow-wave-test.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run deterministic Flow Wave tests
        run: python -m unittest tests/test_flow_wave.py
